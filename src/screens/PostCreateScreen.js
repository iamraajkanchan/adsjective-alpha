import React from "react";
import { View, StyleSheet, Text } from "react-native";

const PostCreateScreen = () => {
  return (
    <View>
      <Text>This is Post Create Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default PostCreateScreen;
